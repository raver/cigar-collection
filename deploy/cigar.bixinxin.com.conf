server {
    # how to use?
    # 1. copy this file to /etc/nginx/sites-available/cigar.bixinxin.com.conf
    # 2. create a symlink to /etc/nginx/sites-enabled/:
    #    sudo ln -s /etc/nginx/sites-available/cigar.bixinxin.com.conf /etc/nginx/sites-enabled/
    listen 80;
    listen [::]:80;
    server_name cigar.bixinxin.com;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name cigar.bixinxin.com;

    # 复用现有的 *.bixinxin.com 证书
    ssl_certificate     /etc/ssl/certs/app.bixinxin.com.pem;
    ssl_certificate_key /etc/ssl/private/app.bixinxin.com.key;

    ssl_session_timeout 1d;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_tickets off;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    client_max_body_size 20m;

    proxy_http_version 1.1;
    proxy_set_header Host              $host;
    proxy_set_header X-Forwarded-Host  $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade            $http_upgrade;
    proxy_set_header Connection         "upgrade";

    # 前端 — SvelteKit
    location / {
        proxy_pass http://127.0.0.1:3100;
    }

    # API — Hono（公开 + 管理）
    location /api/ {
        proxy_pass http://127.0.0.1:3101;
    }

    location /admin/api/ {
        proxy_pass http://127.0.0.1:3101;
    }
}